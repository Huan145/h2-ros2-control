# Trained policies

Place the RL policy here as `policy.pt`.
The controller (`nodes/control/h2_fullbody_controller.py`) loads `models/policy.pt`.

Weights are not committed to git. Download the released policy from this repo's
GitHub Releases, or export your own from the training repo
(UnitreeH2-isaaclab-training) and copy it here.
